import { CynosureDateTimeZonePipe } from './cynosure-date-time-zone.pipe';

describe('CynosureDateTimeZonePipe', () => {
  it('create an instance', () => {
    const pipe = new CynosureDateTimeZonePipe();
    expect(pipe).toBeTruthy();
  });
});
